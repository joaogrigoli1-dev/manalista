# Chave carregada dinamicamente via AWS SSM
# /IA_Equipe_P/claude-api-key (SecureString)
# Configure AWS credentials para acessar o SSM

AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_access_key_id
AWS_SECRET_ACCESS_KEY=your_secret_access_key

# Fallback local (APENAS desenvolvimento — nunca commitar)
# ANTHROPIC_API_KEY=sk-ant-...
